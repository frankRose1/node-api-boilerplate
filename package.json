{
  "name": "node-docker-redis-mongodb-boilerplate",
  "version": "1.0.0",
  "description": "Development for node-redis-mongodb apps made easy with docker.",
  "main": "./src/server.js",
  "scripts": {
    "prestart": "npm run -s build",
    "start": "node dist/server.js",
    "clean": "rm -rf dist && mkdir dist",
    "build-babel": "babel -d ./dist ./src -s",
    "build": "npm run clean && npm run build-babel",
    "dev": "babel-node src/server.js"
  },
  "repository": {
    "type": "git",
    "url": ""
  },
  "author": "Frank Rosendorf",
  "license": "MIT",
  "dependencies": {
    "compression": "^1.7.3",
    "cors": "^2.8.5",
    "express": "^5.0.0-alpha.7",
    "redis": "^2.8.0",
    "mongoose": "^5.6.11",
    "bluebird": "^3.5.5"
  },
  "devDependencies": {
    "@babel/core": "^7.5.5",
    "@babel/node": "^7.5.5",
    "@babel/preset-env": "7.5.5",
    "@babel/cli": "^7.5.5"
  }
}
